# STATE — Scrollback — 2026-07-11

## Now
Per user ("keep building… we are not stopping until we build the complete product"): the **M1 headless lane is fully drained** (capture→OCR→redact→exclusions→chunk→near-dup→dual-ts→retrieval→store schema/migrations→**weekly-shard store w/ provable purge**→key-custody policy). Now building **M2's PURE security primitives ahead** while the env-blocked batch waits: **MCP result spotlighting** done. Next headless candidates (pure, testable, needed regardless): the daemon Unix-socket **request/response contract types** (M2's first task — codable envelope + the recall/list tool shapes, minus the live socket); the filing pipeline's **quarantined-extractor schema + validator** (M3 — schema-validated JSON only, another ambient-as-data boundary); idempotent Notion **`external_key`** derivation (M3, pure). The env-blocked batch (SE key / SQLCipher link / embedding download / live capture + CPU gate / LaunchAgent) still needs the founder machine.

## ⚠️ Environment-blocked batch (needs a code-signed / networked / TCC-granted founder machine — CANNOT be built or verified from this headless shell)
- **Store (b) remainder:** SE key wrapping (`SecKeyCreateRandomKey` SE + `.biometryCurrentSet` + Keychain), interactive Touch ID unlock, FileVault check, **SQLCipher link (vendoring the amalgamation) + wiring the store as the live sink** — the "encrypt before dogfood" step. Policy layer (LOCKED/rate-limit) is done + tested.
- **Embedding (a/b):** bundle llama.cpp + download EmbeddingGemma GGUF (large file, network) + courier bootstrap.
- **Live capture verification:** `ax-dump`/`ocr-dump`/live daemon + the perf/CPU trace (per-binary TCC; grant Terminal.app).
- **LaunchAgent packaging, M1 gate run (8h workday).**
These stay flagged; I keep the pure/testable lanes moving so the founder session is a focused batch of "run + observe + measure", not "build."

## Storage decision (2026-07-11, user-approved)
Build the store on **system SQLite now** (schema/migrations/DAOs done in (a)), turn on **SQLCipher encryption before any real founder data persists** (store (b)). Rationale explained + chosen by the user: SQLCipher is API-identical to SQLite (encrypted = same DB + a key), so this is near-zero rework and keeps momentum; the fiddly vendored-crypto build is safely deferred because there's zero real data until the M1 gate run. Vendored-amalgamation is the eventual destination for the trust/audit-surface story; GRDB was rejected (big dep in the trust-critical daemon).

## Just finished
- **MCP recall-surface safety (pure):** `MCPError` (structured `LOCKED`/`RATE_LIMITED`/`EMPTY_RANGE` — never a silent partial result, §3a) + `QueryThrottle` (anti-hammering on the SEARCH surface, sliding window; separate from `KeyCustodyPolicy`'s unwrap rate-limit — a compromised MCP client can't exfiltrate all memory by rapid-fire query). 220 tests green (+4). Decision-light (spec-mapped) → tests + self-review.
- **Append-only audit ledgers (trust surface — pure DAOs):** `EgressRecord`/`WriteRecord`/`ConsentRecord` + append-only DAOs on `SQLiteCatalogStore` for `egress_ledger` (every network request, append-before-send), `write_ledger` (every Notion mutation → drives archive-undo), `consent_log` (meeting consent). API is append + ordered-read ONLY — no update/delete (enforced by DAO shape + review per the schema; row triggers rejected because they'd fight the tables' `ON DELETE CASCADE`/`SET NULL`; deletion only via whole-shard purge). Added `Statement.intOrNil` for nullable `byte_count`. Appends accumulate immutably + survive reopen. 216 tests green (+7). Decision in docs/decisions.md.
- **Privileged Notion composer (M3 pure primitive — completes the filing pipeline's pure half):** `NotionComposer.compose(validated FilingExtract, title:)` → `NotionPageDraft` of Notion blocks (commitments→to_do checkboxes w/ due dates, log→bulleted, reading→bulleted, empty→placeholder). `NotionBlock` is a CLOSED plain-text set with NO link/image case → the composer structurally can't emit a URL/image (defense-in-depth behind the validator); input type is `FilingExtract` (validated), so it structurally never sees raw captured text. Encoded payload verified to contain no `link`/`http`/`](`. Filing pipeline pure logic now COMPLETE: extractor-output → **validate** → **compose** → [courier commits — env-blocked] with **external_key** idempotency. 209 tests green (+6). Decision in docs/decisions.md.
- **Idempotent filing key (`external_key`, M3 pure primitive):** `ExternalKey.forDraft(recipe:date:destination:)` → deterministic `recipe:YYYY-MM-DD:hash` for the logical write unit (one recipe / local calendar day / destination), so re-filing dedups via the `filing_drafts.external_key UNIQUE` constraint instead of creating a second Notion page (Notion has no idempotency keys). Day resolved in the user's timezone; content-hash deliberately NOT folded in (would break append-on-rerun). 203 tests green (+7); mechanical → tests + self-review. Decision in docs/decisions.md.
- **Filing quarantine validator (write-side injection boundary — M3 pure primitive, built ahead):** `FilingExtractValidator` validates the quarantined extractor's raw JSON fail-closed into `{commitments[],log_entries[],reading[]}` — strict keys/types, bounds, and NO URLs/images (the verified Notion exfil channel); instruction-like text kept as inert data. **Hardened by a bounded 3-lens adversarial review → 1 CRITICAL caught that unit tests missed:** the exfil scan ran on RAW json but `JSONDecoder` resolves `\uXXXX`/`\/` escapes after, so `https:\/\/evil.com` bypassed the scan then decoded to a live link → composer → Notion (total exfil bypass, incl. `![](url)` pixels). Fixed: authoritative scan on DECODED values (raw scan kept as defense-in-depth for ignored fields); regression-tested. Write-side twin of MCP spotlighting; with redact = the 3 ambient-as-data boundaries. 196 tests green (+15); `simulate` unchanged; grep clean. Decision + review in docs/decisions.md.
- **MCP result spotlighting (prompt-injection boundary — M2 pure primitive, built ahead):** `MCPResultFormatter` shapes retrieval `SearchResult`s into the MCP payload, fencing untrusted-ambient snippets (`⟦UNTRUSTED_AMBIENT⟧…⟦/UNTRUSTED_AMBIENT⟧`) with a data-not-instructions notice; trusted snippets plain. The fence is UNFORGEABLE — all content defanged (reserved marker chars stripped) so captured text can't break out (the spotlight analog of SQL-injection escaping). Load-bearing CLAUDE.md invariant ("ambient text is data, never instructions; MCP results spotlighted") made concrete; pairs with redact + provenance as the 3 ambient-trust defenses. Security boundary but small/self-contained + injection vectors directly tested → tests + careful self-review (forge-marker, injected-instructions-stay-fenced, defang-all). 181 tests green (+8); `simulate` unchanged; grep clean. Decision in docs/decisions.md.
- **Weekly-shard store (store c) — one encrypted SQLite file per ISO week + provable purge:** `WeekShard`/`WeekShardCalendar` (pure ISO-week routing math, incl. year-for-week boundary) + `ShardedCatalog` (directory of per-week `SQLiteCatalogStore` files). Writes are episode-atomic (whole episode → its start-week shard, so FKs never cross files); reads fan out across the shards a query's time range touches and re-fuse via `HybridRanker` (single-shard = direct); `purge(before:)` DELETES the whole week files (+wal/shm) before a cutoff → "delete everything before X" is a file unlink, not a row-DELETE (the PRD privacy claim). SQLCipher is a per-shard `key` pass-through. Tested against plaintext temp-dir shards incl. the load-bearing invariant: after purge the file is physically gone AND its data unsearchable. Async `RetrievalStore` conformance = a thin actor wrapper over this sync manager, deferred to live wiring. 173 tests green (+13); `simulate` unchanged; grep clean. Mechanical+well-tested → tests + self-review (purge boundary + provable-delete asserted). Decision in docs/decisions.md.
- **ScrollbackBench retrieval-quality net + bench-driven precision fix:** a `swift test`-wired regression net (`ScrollbackBenchTests`) — seeded synthetic corpus + golden natural-language probes assert recall@3==1.0 + a time-scoped probe. It IMMEDIATELY earned its keep: caught that the FTS sanitizer OR-ing the stopword "the" flooded candidates so recency buried a strong keyword match ("the taco lunch spot" missed the lunch episode) → fixed by dropping a small stopword set in `ftsMatchQuery` (content words never dropped; all-stopword query → recency carries). Real precision win, not fixture-fitting. Synthetic golden set is a placeholder; real founder-dogfood queries seed it at the M1 gate run (data-blocked). 160 tests green (+2 net, +stopword assertions); `simulate` unchanged; grep clean. Decision in docs/decisions.md.
- **Hybrid retrieval (RRF over FTS5 + recency, vector-ready):** pure `HybridRanker` (RRF fusion + per-episode diversification with backfill) + `SQLiteCatalogStore.hybridSearch` (SQL candidate generation — BM25 keyword + recency lists under hard time/app/entity pre-filters — fused via the ranker, `events`-joined for provenance). Both PRD DoDs proven against an in-memory DB: "what did I do today?" (time-scoped, recency-carried) and one-episode-dominant → diversified episodes. Free-text sanitized into a safe FTS5 OR-expression; entity filter via `LIKE ESCAPE`; vector similarity slots in later as a third fused list. **Hardened by a bounded 4-lens adversarial review (correctness/SQL-safety/security-spec/resources) — 1 confirmed of 2 raw:** unclamped `query.limit` could trap on `limit*5` overflow (latent crash) / overflow SQLite's bind cap → clamped at the boundary (`maxResultLimit=200`) + `limit:.max` regression test. Reviewers cleared SQL parameterization, provenance fail-safe, pre-filter exclusion, statement finalization. 158 tests green (+17); `simulate` golden unchanged; grep clean. Decision + review in docs/decisions.md.
- **Dual timestamps (`ts_event` extraction during chunking):** pure `EventTimeExtractor` fills `chunks.ts_event` (the event a chunk refers to) resolved relative to the CAPTURE time — ISO/month-name dates, today/tomorrow/yesterday, and weekdays with next/this/last qualifiers ("moved to Friday" said Monday → that Friday). Precision-first (impossible dates round-trip-rejected; bare numbers/versions/ports never match), earliest-reference-wins, deterministic (injected timezone; NSDataDetector rejected — resolves against now, non-deterministic + semantically wrong for re-reads). Wired into `Chunker.chunk`; the `chunks.ts_event` column + nullable DAO binding already existed. 141 tests green (+15); `simulate` golden unchanged; grep clean. Pure/mechanical → tests + self-review. Decision + "may"-ambiguity limitation in docs/decisions.md.
- **Near-dup collapse (MinHash + LSH, pre-embedding):** pure `MinHasher` (128 affine permutations over FNV-1a-32 word-trigram shingles; fixed-seed → byte-identical signatures across runs) + streaming `NearDupCollapser` (LSH banding surfaces candidates, an exact signature-Jaccard ≥0.85 verify enforces the collapse — the two-stage gate that keeps distinct memory from being silently dropped). Wired into `ChunkingStage` after the exact-hash miss (`nearDupSkips` counter; default on, `nearDup: nil` disables). Catches the scrolled/edited screen re-read that byte-hash dedup misses; a mixed repetitive stream collapses 60% of chunks. Correctness-checked (overflow-proven `a·x+b<2⁶⁴`, `UInt32(h)` never traps, determinism asserted) — an algorithmic/volume component, not a security gate, so tests + focused self-review (heavy adversarial reviews stay on redact/exclusions/retrieval). 126 tests green (+15); `simulate` golden unchanged; grep clean. Decision + rejected-alternatives in docs/decisions.md.
- **Key-custody policy (store b, testable half):** `KeyCustodyPolicy` (pure, clock-injected) — session timeout → LOCKED on expiry, sliding-window rate-limit on unwrap attempts (hammering trips it; a successful unlock resets it). Covers 2 of 3 (b) DoD points; the SE-hardware unwrap + SQLCipher link are the environment-blocked remainder above. 111 tests green (+8); `simulate` golden unchanged; grep clean.
- **Encrypted store (a) — schema + migrations:** `SQLiteDatabase` (thin sqlite3-C wrapper; the SQLCipher swap is one isolated `PRAGMA key` line) + `CatalogSchema` (full tech-spec §2 schema as forward-only migration v1, minus `chunks_vec`) + `SQLiteCatalogStore` (idempotent migrate, episode/event/chunk DAOs, FK cascade, FTS5 search). System SQLite for now; SQLCipher + live wiring land with (b). 103 tests green (+7); `simulate` golden unchanged; grep clean. Mechanical/well-tested → committed on tests+self-review.
- **Chunker + volume instrumentation:** pure `Chunker` (event text → 512–1024-token chunks; sentence-pack + word hard-split; pluggable token estimate until the real EmbeddingGemma tokenizer lands) + `ChunkingStage` (normalized-hash dedup so identical re-reads store once; total + per-hour volume counters; chunksStored = the vector count feeding the sqlite-vec ~1M re-eval trigger). Wired live via `InstrumentedSink` (logs volume on shutdown; chunks not persisted until the store lands). 96 tests green (+9); `simulate` golden unchanged; grep clean. Mechanical/well-tested → committed on tests+self-review (heavy reviews reserved for key custody / retrieval).
- **Default-on exclusions:** pure `ExclusionSet` (app/url/window/regex rules × neverCapture|redact, strictest wins) honored in `CaptureEngine` — a `neverCapture` app opens no episode and stores nothing across app-switch/clipboard/resume/fallback; `redact` records the episode but stores a placeholder (provider never read). Defaults: password managers + Claude Desktop + incognito titles (NOT ordinary apps — golden line safe). Runtime signals `IsSecureEventInputEnabled` + `NSWindowSharingNone` gate the live AX/OCR providers (`CaptureGuards`, daemon-only, not CI-observable). This closes the OCR secure-field residual for excluded apps. **Hardened by a 4-lens review (4 findings fixed, no gating gap):** sharing-none guard now fail-safe on any pid window opting out (was: wrong/topmost window); redact masks the episode window title too (was: verbatim leak); `.url`/`.schedule` documented inert (no URL/clock plumbed — browser-URL extraction TODO'd); redact-clipboard path tested. 87 tests green (+13); `simulate` golden unchanged; grep clean. Decisions + review fixes in docs/decisions.md.
- **Redact stage (capture → redact → chunk):** pure `Redactor` at the `CaptureEngine.emit` chokepoint masks high-risk secrets (PEM keys, `sk-`/`gh_`/`AKIA`/`AIza`/`xox-`/JWT tokens, Luhn-valid cards) → `[redacted:<name>]` and sets `CaptureEvent.redactionFlags` (schema `events.redaction_flags`). PII (emails/phones/names) deliberately preserved — defense-in-depth, not a perfect-redaction promise (PRD:147). One redaction pass drives rawText+hash+flags; clipboard now stored normalized (verbatim dropped). **Hardened by a 4-lens review (7 findings fixed):** private-key ReDoS (bounded quantifier + `contains` pre-gate → BEGIN-flood <1s not O(n²)); clipboard whitespace-card leak (single pass + normalized store); card greedy over-match (`isRedactableCardRun` prefix/suffix Luhn). 74 tests green (+23); `simulate` golden unchanged; grep clean. Decision + rejected-alternatives + review fixes in docs/decisions.md.
- **OCR fallback (AX-opaque surfaces):** `LayeredTextSnapshotProvider` composes AX + OCR behind the (synchronous) `TextSnapshotProvider` seam, routed by a per-app capability matrix (`AppCaptureCapabilities` → `CaptureStrategy`) and `OCRFallbackPolicy` (OCR only on empty/thin AX, never regressing below AX). Live path `VisionOCRExtractor` = ScreenCaptureKit window screenshot → Vision text recognition → `OCRTextAssembler` reading-order join; the CGImage is discarded post-recognition (zero frames stored). Degrades to AX-only without the Screen Recording grant. New `scrollbackd ocr-dump` diagnostic. Decision + secure-field-bypass gotcha logged in docs/decisions.md.
- **Adversarial 5-lens review → 12 confirmed findings, all fixed:** the `OCRTextAssembler` comparator was not a strict weak ordering (intransitive → `sorted` garbled reading order on dense/staggered layouts) — fixed to a total order + per-line left sort; a timeout-path data race on the async→sync bridge — fixed (read only on `.success`) plus an in-flight guard + task cancel; OCR bypassed the AX secure-field guard — added `CapturedText.containedSecureField` so OCR is refused for a window where AX skipped a secure field; Retina 1× undersampling — capture at pixel scale; `bestWindow` picked largest not focused — prefer title match. 51 tests green (+20 total for the increment); `simulate` exact; networking grep clean; permission-denied degradation observed (exit 3).
- Prior: capture perf pass (batched AX reads, focused-window cache, title debounce); event-driven capture spike + 8-angle review hardening.

## Blocked / Open questions
- **Founder actions, time-critical:** register getscrollback.com (squattable); enroll Apple Developer Program (lead time).
- **Dated:** Otter.AI MTD hearing Jul 15 — tripwire task in TODO Now.
- **Live-TCC verification pending a re-granted binary:** rebuilds drop the dev binary's Accessibility grant, and OCR additionally needs the Screen Recording grant — so `ax-dump` / `ocr-dump` / live capture and the perf/CPU trace need a manual granted run (folds into the M1 gate run). Automation stays on `simulate`.
- **OCR secure-field bypass (partially mitigated):** a screenshot has no field-level secure-field guard. Now suppressed for windows where AX saw a secure field (`containedSecureField`). Residual: all-canvas logins with empty AX + `ocrOnly` apps — closed by the NEXT tasks (default exclusions + redact). Must NOT ship to real dogfood before default exclusions land. (Logged in decisions.md.)

## Next up
- Redact-mode stage → default-on exclusions (password managers/banking/incognito/secure-input — closes the OCR secure-field gap at the app level) → chunker + capture-time dedup + volume/vector counters → encrypted store (SQLCipher + SE key) before real dogfood data persists.
